# formula-profile
Recipe to build profile functionality for Drupal.

This recipe is intended to be standalone within the Formula recipe stack.

- It explicitly installs module dependencies required by its configuration.
- It includes taxonomy vocabularies used by profile fields (`department`, `profile_type`) so profile classification works out of the box.
