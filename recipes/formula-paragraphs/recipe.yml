name: '🦄 🌈  Formula: Paragraphs'
description: 'Installs the Paragraphs module stack with foundational types: section, separator, and rich_text. Individual paragraph types (e.g. formula-cta) depend on this recipe.'
type: 'Paragraphs'

install:
  - paragraphs
  - layout_paragraphs
  - nomarkup

config:
  strict: false
